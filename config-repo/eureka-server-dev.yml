server:
  port: 10086
eureka:
  client:
    service-url:
      defaultZone: http://localhost:${server.port}/euerka
    register-with-eureka: true
    fetch-registry: true
  instance:
    hostname: localhost